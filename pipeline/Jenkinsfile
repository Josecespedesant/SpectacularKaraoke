pipeline{
  agent any
  
    stages{
      stage ("Build"){
        steps {
          sh "rm -rf *.tar.gz"
          sh "npm install /API"
          sh "tar czf SpectacularKaraoke-$BUILD_NUMBER.tar.gz node_modules API Diagrams"
        }
        
      }
      
    }
  
}
